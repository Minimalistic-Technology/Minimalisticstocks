import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['example.com', 'assets-netstorage.groww.in', 'static.vecteezy.com', 'previews.123rf.com', 'assets-netstorage.groww.in', 'img.icons8.com','via.placeholder.com'],
  },
};

export default nextConfig;
